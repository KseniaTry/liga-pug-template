mixin rate(starsCount, rate)
  div.work-card__rate.rate
    span.visually-hidden= `${rate} звезды`
    - for (var i = 1; i <= rate; i++)
      span.rate__star.rate-active
    - for (var i = 1; i <= starsCount - rate; i++)
      span.rate__star

